#include <iostream>

typedef struct node{
	int data; 
	struct node * l;
	struct node * r;
	struct node * root;
	

}node;


Node * append ( node * root, int data){
	
	if (root == NULL){
		root = new node();
		root->l = NULL;
		root->root = NULL;
		root->rith = NULL;
		root->data = data;
		return root;
	}

	else {
		
		node *prtR = root;
		while (prtR->r != NULL && prt->l )
		
		
	}
	
	
}